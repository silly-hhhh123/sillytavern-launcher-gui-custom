# 更新日志

本文件记录 SillyTavern Launcher GUI 的所有版本更新内容。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。

---
## [1.0.8] - 2026-04-10

## 修复
- **BUG**：解压 tar.gz 时，原代码先将所有 entries 收集到 Vec 中，此时底层文件流读取位置已移至末尾，再调用 entry.unpack() 时无法读取数据，导致解压失败。

## 感谢

- **willflier**：感谢大佬提交的PR，完善MacOS端的可用性。

## [1.0.7] - 2026-04-09

### 修复
- **内置NodeJs**：修复了MacOS上内置NodeJs解压失败的问题。
- **Data数据存放位置**：修复了MacOS上Data数据存放位置不正确的问题。

## [1.0.6] - 2026-04-08

### 移除

- **炫酷 API**：按照作者要求，移除了炫酷 API公益站，如果需要请自行寻找替代品。

## [1.0.5] - 2026-04-07

### 修复

- **非 Windows 平台 Node.js 安装失败**：修复了在 macOS 和 Linux 上使用内置功能下载 Node.js 时，因官方仅提供 `.tar.gz` 格式而硬编码请求 `.zip` 导致下载报 404 网络错误的问题。现在会自动识别操作系统并请求对应后缀的压缩包，并引入 `tar` 与 `flate2` 依赖进行解压。
- **Node.js 解压权限丢失**：修复了在 Unix 系统（macOS/Linux）下解压 Node.js 压缩包时丢失可执行文件权限的问题，现已确保解压时自动保留（或恢复）`node` 二进制文件的执行权限。
- **多显示器 DPI 缩放显示异常**：修复了当用户拥有多个显示器且缩放比例不同时，将软件窗口跨屏拖拽会导致界面显示不全或撑满变形的问题。通过在 `build.rs` 中补回 Windows 原生的 `PerMonitorV2` DPI 缩放感知配置彻底解决了此问题。

### 优化

- **窗口尺寸保护与拉伸支持**：移除了原有的固定宽高限制（1200x800），现在软件窗口支持自由拉伸。同时设置了最小尺寸（960x640）和最大尺寸（1600x1000）的安全边界，并禁用了全屏最大化，确保各种分辨率屏幕下 UI 始终能优雅显示。
- **增强启动环境检测与提示**：
  - **系统版本检测**：在启动时如果检测到低于 Windows 10 的旧版系统（如 Win7/Win8），会弹出警告弹窗提示用户 Tauri 框架可能无法正常工作。
  - **WebView2 运行时检测**：如果检测到用户的 Windows 系统未安装 Edge WebView2 运行时，会直接弹出原生错误对话框，点击“确定”可自动前往微软官网下载。
  - **Visual C++ 运行时检测**：如果检测到缺失 VC++ Redistributable，同样会弹窗提示，并在点击“确定”后自动下载匹配当前系统架构（x64/x86/arm64）的最新安装包。
- **静态链接 C++ 运行时库**：优化了 Windows 版本的 Rust 编译配置，开启了 `+crt-static` 静态链接，从而大幅减少软件底层对系统 `vc_redist` 的依赖，降低了“无法定位程序输入点”或“找不到 DLL”等问题的发生概率。

---

## [1.0.4] - 2026-04-05

### 修复

- **进程端口占用报错**：修复了在执行软件更新并重启后，由于旧酒馆进程的端口未及时被操作系统释放，导致新实例启动失败的问题（增加重启缓冲时间）。
- **更新后数据丢失错觉**：修复了由于 Tauri 更新器重启应用时工作目录变更，导致 `config.json` 路径读取错误，进而引发“看不到已安装的酒馆版本”和“本地扫描把角色卡删了”等严重路径读取错误的问题。
- **角色卡与世界书目录规范**：修复了“角色卡”和“世界书”的默认读取和写入目录问题。现在统一强制读取并写入 `data/st_data/default-user/characters` 和 `data/st_data/default-user/worlds`，移除了对旧根目录的回退机制，保持数据结构完全统一。

### 优化

- **资源目录缺失提示**：在“资源管理”页面，如果检测到角色卡或世界书对应的 `default-user` 目录不存在，前端现在会显示友好的提示信息：“目录不存在。请启动一次酒馆 或 进行资源迁移”，而不是简单地显示为空列表。

---

## [1.0.3] - 2026-04-05

### 新增

- **酒馆数据模式 (Data Mode)**：在基础设置中新增「酒馆数据模式」选项，支持切换「全局模式 (st_data)」和「当前酒馆独立数据模式 (data)」，并实现了对应的配置路径与数据迁移逻辑的隔离，更灵活地管理不同版本实例的数据。

### 修复

- **数据模式翻译丢失**：修复了在设置界面「酒馆数据模式」选项中显示为翻译字段变量（`settings.dataMode`）而不是实际中文文本的问题。
- **AppTooltip 层级缩进修复**：修复了 `Tavern.vue` 配置文件页面中，部分跨域（CORS）配置移除按钮的 `AppTooltip` 悬浮提示组件缩进异常的问题。
- **界面窗口样式重构**：更新了 `Oheader.vue` 组件中的图标显示和顶部/左侧边栏的排版布局与间距，提升了界面在不同分辨率下的兼容性和整体美观度。

---

## [1.0.2] - 2026-04-05

### 新增

date

- **鼠标悬浮提示（AppTooltip）**：新增统一悬浮提示组件，所有页面按钮均添加鼠标悬停提示，长文本自动换行，浮动到 body 顶层不惧父容器 overflow 裁剪。
- **对话历史管理**：资源页面新增「对话历史」标签页，按角色分组展示聊天记录，支持气泡预览与批量勾选删除。

### 优化

- **GitHub 加速稳定性**：加速服务停用后自动还原全局 Git 代理配置；新增 gzip 支持检测；缺 Git 时直连测试改为直接报错而非静默失败。
- **Git / Node 安装进度可视化**：安装进度状态改为全局 reactive 单例，前端一次性订阅，所有安装场景统一展示进度。
- **依赖缺失自动修复**：运行时检测到 node_modules 缺失依赖时自动触发 `npm install` 补装，stderr 捕获缺包错误并触发修复事件。
- **扫描引擎健壮性**：移除 jwalk 线程中的 `metadata()` 调用避免死锁；扫描过程可实时查看日志弹窗。

### 修复

- **命令行窗口闪动**：Windows 上设置代理和进行 GitHub 连接测试时，消除命令行窗口一闪而过的问题（`CREATE_NO_WINDOW` + `Stdio::null()`）。
- **AppTooltip 全局失效**：修复 Versions.vue 和 Console.vue 使用 Tooltip 组件但未 import 的问题，以及 Tavern.vue / Resources.vue 中 prop 名错误（`:title=` 应为 `:text=`）导致提示不显示的问题。

---

## [1.0.1] - 2026-04-03

### 修复

修复在特定情况下，配置信息会被覆盖的问题。

## [1.0.0] - 2026-04-02

### 正式版发布

感谢各位的支持，酒馆启动器正式发布第一个正式版版本。

虽然之前好像非常马虎的把 一些地方的版本号都写成了正式版的版本号（尴尬的笑），
所以干脆直接发布正式版好了，毕竟已经完成了大部分功能，而且也经过了不少测试，所以就先发布了正式版吧。

> [!WARNING]
> **Windows 用户注意**：如果安装程序提示权限不足或安装失败，请右键点击安装文件，选择「**以管理员身份运行**」后重试。

> [!WARNING]
> **其他平台 用户注意**：因为测试的设备平台有限，目前所有的基本功能在 Windows 下应该没有问题了，在其他平台上的表现可能还有待测试。

### 修复

- **局域网服务模式**：修复局域网服务模式下，获取到错误的 IP 地址的问题。

## [0.1.8] - 2026-04-02

### 修复

- **MacOS**：紧急修复MacOS端构建失败的问题。

## [0.1.7] - 2026-04-02

### 优化

- **控制台日志**：优化控制台日志显示，避免抽出。

## [0.1.6] - 2026-04-01

### 新增

- **鸣谢页面国际化**：设置页面新增「鸣谢」标签页，展示项目使用的技术栈与第三方库，描述文本支持中英文双语显示。
- **启动器网络代理**：在设置新增网络代理设置，可选择跟随系统代理或自定义代理地址，启动酒馆时自动应用代理设置。
- **GitHub 访问测试**：在 设置页面的 GitHub 加速 和 代理设置下 新增 Github所有服务 访问检测的功能，结果能直观显示各种服务的连接状态（如是加速地址无法访问特定资源）；新增下载速度测试功能，直观展示加速效果。

### 优化

- **私人扩展凭据提示**：安装私人（无加速）扩展时正确显示 Git 凭据输入窗口，不再被全局环境变量拦截。
- **扩展离线修复保底**：当扩展所在仓库无法连接且系统无 Git 可执行文件时，自动生成最小化 .git 骨架，确保离线状态下扩展仍可使用。

### 修复

- **GitHub 加速静默失败**：修复加速地址可用但无法加速特定资源时无任何提示的问题，现会显示具体警告信息。

---

## [0.1.5] - 2026-03-30

### 新增

- **多种启动模式**：新增调试模式（开启 Node DevTools Inspector）、桌面程序模式（内嵌窗口，无需打开浏览器）、局域网模式（仅内网访问）、公网模式（全网可访问）共四种启动模式，可在设置页面切换。
- **局域网 / 公网链接面板**：局域网或公网模式启动酒馆后，首页按钮切换为「局域网链接」/「公网链接」，点击弹出专属面板，展示 IPv4 / IPv6 双二维码、地址与端口，一键复制或在浏览器打开。
- **公网可用性检测**：公网模式下，面板打开后自动通过 itdog TCPing（基于 Chrome Headless）检测 IPv4 / IPv6 节点超时率，并标注推荐协议。检测结果按服务启动次数缓存，同一次公网服务运行期间重复打开面板无需重新检测，点刷新按钮可强制重测。无 Chrome 时自动降级，提供 itdog 链接供手动查看。
- **对话历史管理**：资源页面新增「对话历史」标签页，按角色分组展示所有聊天记录，支持点击预览对话气泡、批量勾选删除。
- **GitHub 加速预加载与静态回退**：启动器启动时静默预加载 GitHub 加速地址列表；API 不可用时自动回退到内置备用加速地址，确保用户始终有可用选项。
- **GitHub 加速与 Node 版本联动**：Node ≥ 18.19.0 时使用 `--import` 拦截器注入加速；低版本自动改用 `git config --global url.xxx.insteadOf` 全局代理，服务停止时自动还原。
- **配置迁移**：在酒馆配置页面新增「迁移配置」按钮，支持一键迁移你之前使用的配置直接迁移到覆盖掉当前统一用户下配置。
- **资源迁移**：在资源页面新增「资源迁移」按钮，支持把你添加的酒馆里的角色卡、世界书等资源迁移与合并的形式，合并到当前统一用户下。

### 优化

- **扫描引擎升级**：采用 jwalk（并行）+ walkdir（顺序）双引擎策略，jwalk 5 秒内无结果自动切换 walkdir，彻底解决因权限问题导致扫描卡死的问题。
- **首次扫描仅执行一次**：首次启动完成自动扫描后不再重复触发，用户仍可手动发起扫描。
- **扫描路径实时日志**：扫描过程中可点击日志按钮实时查看当前扫描路径，方便排查异常。
- **扩展按需加载**：移除启动时预加载扩展列表，改为进入扩展页面时按需加载，加快启动速度。
- **桌面模式关闭体验**：桌面模式关闭子窗口时正确识别为主动停止，不再误报「异常退出」。
- **Git和Nodejs 环境切换功能**：提供自由切换到系统环境 或 内置环境。

### 修复

- **扫描大小写**：修复 `SillyTavern`（官方大写）无法被扫描识别的问题。
- **扫描误识别**：修复将安装目录下 `data/sillytavern/` 路径误识别为本地实例的问题。
- **手动添加酒馆**：修复 `package.json` 读取返回类型不匹配导致添加失败，以及 name 字段大小写校验不匹配的问题。
- **扩展修复路径**：修复第三方扩展自动修复和手动修复时路径错误的问题。

## [0.1.4] - 2026-03-29

### 修复

- **手动添加酒馆**：修复1.0.3版本中，手动添加酒馆时，无法正确识别 `package.json` 文件的问题。

## [0.1.3] - 2026-03-27

### 新增

- **小白自动化一键安装**：针对全新小白用户新增全自动化“一键安装并启动”功能，串联 Node 环境安装、本体下载与一键启动的全流程。
- **本地实例极速扫描**：新增后台多线程扫描功能（基于 `jwalk` 和 `rayon`），静默全盘搜索本地已有的 SillyTavern 实例。并在设置中增加扫描时可使用的 CPU 核心数选项。
- **版本管理重构**：版本页面重组为“本地已安装”与“在线下载”双标签页，支持本地多路径实例的隔离管理和一键切换。
- **已有版本关联**：在版本管理页面新增“选择已有酒馆”按钮，可通过选择现成的 `package.json` 将其无缝连接到启动器中。
- **Git 环境管理**：在设置页面新增 Git 环境检测与跨平台一键安装支持。Windows 支持优先使用 Winget，并提供 PortableGit 备用下载方案。
- **国内网络优化自动配置**：新增区域检测，当系统地区为中国境内（含港澳台）时，初次启动将自动开启 GitHub 代理并自动将 npm 镜像源切换为淘宝源。
- **Git 集成**：提供轻量Git集成，方便让酒馆扩展支持自动更新。
- **Github 加速**：在酒馆里安装扩展时候请求的Github仓库时候，会自动替换成加速地址，如果你不需要可以在设置里关闭加速。

### 优化

- **本地版本切换优化**：不再依赖软链接创建目录，改为直接保存本地绝对路径。切换版本时自动更新程序的工作目录，避免因跨盘符或权限问题导致的启动失败。
- **启动器界面优化**：调整部分 UI 元素位置，优化启动器界面布局。
- **酒馆设置优化**：在安装依赖后自动生成优化过的设置文件，设置 关闭了酒馆的欢迎页面、两边面板锁定打开等优化。

### 移除

- **Soul公益站**：由于Soul公益站是类脑社区作者搭建的，并没有授权公开到类脑之外的地方，所以如果需要请到类脑社区自行查找使用。

## [0.1.2] - 2026-03-24

### 新增

- Tavern 配置页面完整国际化支持（中英文）
- 内存缓存容量限制数字输入框（自动拼接 MB 单位）
- SSL 证书文件选择器（支持点击按钮选择文件）
- CORS 配置可视化列表 UI（支持动态添加/删除项）
- 所有数组类型配置改为列表形式 UI（IP 白名单、主机白名单、域名白名单等）
- 状态指示器国际化（同步中/就绪/正在加载配置）

### 优化

- 替换所有硬编码 SVG 图标为 @phosphor-icons/vue 组件
  - 删除按钮统一使用 PhX 图标
  - 添加按钮统一使用 PhPlus 图标
  - 文件选择按钮使用 PhFile 图标
- 折叠面板默认展开重要配置板块（SSL、CORS、代理与备份等）
- 缩略图尺寸输入框添加最小宽度限制，防止小屏幕重叠
- 代码量减少约 95%（SVG 迁移至图标组件）

### 修复

- 修复 IPv4/IPv6监听地址翻译键缺失问题
- 修复内存缓存容量需要手动输入 "mb" 后缀的问题
- 修复缩略图优化设置中输入框在小屏幕上重叠的问题
- 修复部分 UI 文本未国际化的问题

### 技术改进

- 实现 Vue 双向绑定自动处理单位后缀
- 使用 watch 监听实现配置实时同步
- 统一图标库使用，提升代码可维护性
- 完善翻译键管理，避免重复定义

## [0.1.1] - 2026-03-22

### 优化

- 缩短启动与停止服务的响应时间
- 改进日志输出格式，增加时间戳
- 调整扩展管理界面加载速度
- 实现自动更新功能

## [0.1.0] - 2026-03-22

### 新增

- 首次发布 SillyTavern Launcher GUI
- 一键启动 / 停止 SillyTavern 服务
- 可视化版本管理（稳定版 / 开发版切换）
- 扩展管理功能
- 多语言支持（简体中文 / English）
- 客户端自动更新功能（静默升级）
- 深色 / 浅色主题切换
- 系统托盘最小化支持
